# 六面世界 · 前端 UI 结构完整清单（设计交接文档）

> 用途：把本文档交给设计 AI / 设计师，在**真实结构**上出原型稿，避免凭空设计。
> 生成基准：`renderer/index.html`（212 行）+ `renderer/settings.html`（254 行）+ `renderer/styles.css`（约 1560 行）+ `renderer/app.js`（约 3259 行），2026-08-26。
> 代码引用格式：`文件:行号` 或 `文件 §样式区段名`，可交叉核对。

---

## 0. 如何使用本文档

1. **不要一次喂给设计 AI 全文**。每次投喂 = 「第 1-2 章全局设计语言」+「单个界面章节」，让它在该界面的真实信息架构上重排视觉。
2. **现成的投喂提示词模板见第 10 章**（含输出格式要求、占位符对照表、迭代反馈模板、验收流程），复制替换占位符即可用。
3. 每个界面要求产出：**浅色 + 深色双版本**、桌面尺寸（主窗口建议 1120×700，最小 500×420）、标注使用的 design token（第 2 章变量名）。
4. 第 9 章给出了建议的重设计优先级，可按序开展。

---

## 1. 产品概述

- **是什么**：独立 Electron 桌面应用「六面世界 · Codex」——《无职转生》同人向 AI 文字 RPG / 人生模拟器。用户自带 OpenAI 兼容 API Key，AI 以「甲龙历」纪年推进叙事，每回合输出 A/B/C 选项按钮；可选插图模型为场景生成小说插图。
- **核心循环**：读叙事 → 点选项 / 自由输入 → AI 推进 →（可选）生成插图 → 不满意可开 IF 分歧线回溯重选。
- **关键概念**：工作区（世界，多套独立存档）＞ 世界线（会话，可 IF 分支）＞ 消息（幕）。画廊 = 当前工作区全部插图集。
- **设计基调**（styles.css §1）：Codex 式极简——安静、留白、少装饰、琥珀色 accent、等宽字体点缀。

---

## 2. 全局设计语言（Design Tokens）

### 2.1 颜色（CSS 变量，styles.css:2-64）
| 变量 | 用途 | 深色值 | 浅色值 |
|---|---|---|---|
| `--bg` | 窗口底 | #191919 | #fafaf8 |
| `--panel` / `--panel-2` | 面板 / 次级面板 | #212121 / #2a2a2a | #fff / #f1efea |
| `--border` / `--border-strong` | 边框 / 强边框 | #303030 / #3e3e3e | #e4e1da / #cfccc4 |
| `--text` / `--text-dim` / `--text-faint` | 主字 / 次字 / 弱字 | #d6d3cd / #97948e / #6c6a65 | #26231e / #6b675f / #a09c93 |
| `--accent` / `--accent-dim` / `--accent-glow` | 琥珀主色 / 暗版 / 光晕 | #c98b4b | #a5641f |
| `--on-accent` | accent 上的文字 | #241c10（深字） | #fff |
| `--danger` / `--ok` | 危险 / 成功 | #e06b6b / #7fb069 | #c84646 / #4e8a3f |
| `--surface-1..3` | 海拔：卡片/浮层/顶层弹窗 | 由 --panel 推导 | 同 |
| `--shadow-1..3` | 三档投影 | 黑基 | 暖灰基 |

### 2.2 主题系统（双轴）
- **明暗轴**：`html[data-theme=dark|light]` + 跟随系统（matchMedia）。
- **调色板轴**：`html[data-palette]` 7 种预设（styles.css:1179-1270）：codex 琥珀（默认）/ paper 羊皮纸 / forest 林间 / violet 紫晶 / ocean 海渊 / rose 蔷薇 / contrast 高对比。每种 × 明暗 = 14 套。
- **样式轴**（用户可调，settings）：展示字体 sans/serif/mono/kai；圆角 none/small/standard/round；文字密度 compact/standard/relaxed；字号 small/standard/large；内容宽度 640/720/860/980px。
- **布局轴**：sidebar 标准 / focus 专注（隐侧栏）/ immersive 沉浸（隐侧栏+极简头部）；侧栏左/右。

### 2.3 图标与组件规范
- 图标：内联 SVG，16px 网格 / 1.5px stroke / currentColor（styles.css §R66）。窗口按钮、导航均用线性图标；文字按钮直接用汉字（画廊/置顶/主题）。
- 组件状态统一（styles.css §R67）：hover 提亮背景、active 按压、disabled 降透明；键盘焦点环仅 Tab 可见（§键盘焦点环）。
- 动效：弹层统一进出场（fadein/fadeout/modalin，§弹层统一进出场）；向导切步方向滑入（wizfwd/wizback）；toast 右上滑入。
- 字体栈：`--mono` SF Mono/Cascadia/Consolas；`--sans` Segoe UI/PingFang/微软雅黑。

### 2.4 氛围设计语言（R76 新方向：Mineradio 电影场 + 灵动岛，用户 2026-08-26 确认）
> 视觉基线原型：`scripts-dev/proto/reference-island-cinema.html`（双主题、可交互演示）。截图：`scripts-dev/shot-proto-island-*.png`。

在 2.1-2.3 的 token 之上**叠加**以下氛围层（不推翻现有变量，新增语义）：
| 新 token（提案） | 深色值 | 浅色值 | 用途 |
|---|---|---|---|
| `--stage` | #0b0b0d | #f3f1ec | 电影场底色（比 --bg 更深/更暖） |
| `--glass` / `--glass-strong` | rgba(28,28,32,.58/.78) | rgba(255,255,255,.66/.86) | 玻璃面板底（backdrop-blur 配套） |
| `--glass-border` / `--glass-highlight` | rgba(255,255,255,.08/.10) | rgba(38,35,30,.08) / rgba(255,255,255,.85) | 玻璃描边 / 内高光 |
| `--accent-glow`（增强） | rgba(217,154,82,.34) | rgba(165,100,31,.22) | 环境光晕、插图溢光 |
| `--ease-spring` | cubic-bezier(.32,1.35,.35,1) | 同 | 弹性动效曲线（灵动岛/悬浮/勾选） |

五个关键手法：
1. **电影暗场**：底色加深；顶部中央叠一层从最新插图取色的环境光晕（radial-gradient，随内容呼吸）；全局 3-5% 胶片噪点（SVG feTurbulence）。
2. **玻璃拟态**：状态面板/选项按钮/悬浮层 = 半透明底 + backdrop-blur(16-28px) saturate(1.3-1.5) + 1px glass-border + inset 顶部高光。
3. **舞台化插图**：插图是画面主角——大圆角(18px)、环境光晕外溢（box-shadow 0 18px 70px accent-glow）、hover 流光扫过。
4. **弹性动效**：悬浮/展开/勾选用 spring 曲线（带 8-12% 过冲）；灵动岛 morph .5s spring。
5. **排版**：叙事正文直接排在暗场上（去卡片框），行高 1.85；场景行 = 镜头标注（accent 短线 + 等宽 + 字距 1.5px）；状态面板 = 玻璃 HUD。

**灵动岛通知（替代 L6 Toast）**：顶部居中悬浮胶囊（top 46px，z 1200，同现 toast 位置）。
- 收纳态：圆点图标 + 一句话，胶囊 999px 圆角，玻璃底 + blur(28px)。
- 四态：**busy**（accent 呼吸光环 + 底部不确定流光线 + 停止按钮）/ **ok**（绿勾）/ **插图完成**（morph 展开为带缩略图卡片 + 「查看」按钮）/ **err**（红染 + 重试按钮）。
- 动效：入场 translateY(-26px)+scale(.86) spring 弹入；状态切换宽高/圆角 morph（.5s spring）；busy 12s / 其他 6s 自动收纳离场。
- 交互：点击「查看」跳转对应消息/画廊；悬停展开详情行。

**启动入场动画（R76，Mineradio 式电影开场）**：配方已从用户本地安装的 Mineradio（D:\Mineradio）源码 `public/css/index.css` 逐帧提取——五层舞台（近黑 #010304 / 对角光带+扫描线 / Canvas 粒子尘埃 / 四缘暗角 / feTurbulence 噪点 + inset 180px 景深暗角）、字标两段式序列（主词 clip-path 细缝展开+斜切校正+字距收紧+左移让位；副词右侧切入+渐变流光扫字 background-clip:text+色散 RGB split 阴影-2px 珊瑚/+2px 青+金色点缀字）、信号线四段 scaleX 过冲+blip 光点穿越、副标 .22em 字距、点击进入呼吸脉冲、离场 scale(1.010)+620ms 双层时序；骨架 5200ms/4200ms，缓动 cubic-bezier(.22,1,.36,1)；色彩三角 琥珀#d99a52/青#7ad7c2/珊瑚#ff5367。完整提示词：`design-prompts/p1-splash-entrance.md`。

---

## 3. 窗口与界面总览

| # | 界面 | 载体 | 常驻/弹层 |
|---|---|---|---|
| W1 | 主窗口 | index.html | 常驻 |
| W2 | 设置窗口 | settings.html（独立系统窗口） | 按需 |
| L1-L11 | 弹层 ×11 | 主窗口内 | 见第 6 章 |

主窗口布局骨架（index.html:10-110）：
```
titlebar（无边框窗自绘标题栏）
└─ .layout
   ├─ aside.sidebar（工作区/世界线/搜索/内核状态/设置入口；可收起、可拖宽、可右置）
   └─ main.chat
      ├─ .progress-rail（仅侧栏完全收起时显示）
      ├─ .chat-header（世界线标题 + 忙碌状态徽标）
      ├─ .search-bar（Ctrl+F，默认隐藏）
      ├─ .messages（消息流 / 空状态）
      ├─ .choices（选项按钮区，可收起）
      └─ .composer（输入框 + 配置簇 + 动作簇）
```

---

## 4. 主窗口逐区块详述

### 4.1 标题栏 `.titlebar`（index.html:10-25，styles.css §标题栏）
- **左**：侧栏收起按钮（⟨ 图标，Ctrl+B）+ 菱形 sigil 标志 + 「六面世界」窗名。
- **右**：文字按钮 画廊 / 置顶 / 主题 ＋ 分隔线 ＋ 窗口控制 最小化/最大化/关闭（SVG，关闭悬停红）。
- 状态：生成中时标题栏出现忙碌徽标（Codex 式，app.js:1999）；置顶激活时「置顶」按钮高亮。
- 整栏为窗口拖动区（-webkit-app-region: drag）。

### 4.2 侧栏 `.sidebar`（index.html:28-53）
自上而下：
1. **工作区切换器 `.ws-btn`**（app.js renderWsMenu:539）：菱形图标 + 工作区名 + 下拉箭头。点击弹出 **`.ws-menu`**：工作区列表（当前项高亮、每项含世界线数 meta）+ 分隔线 + 操作项：新建 / 重命名 / 设置专属内核… / 删除（红色，默认工作区不可删）。
2. **「新世界线」主按钮 `.new-chat`**（Ctrl+N）。
3. **工作区内搜索框 `.sb-search`**：placeholder「搜索本工作区…」，命中时会话项下显示命中摘要（.session-hits），Esc 清空。
4. **世界线列表 `.session-area`**（renderSessionList:358）：
   - 标签行「世界线」+ 收起按钮。
   - 会话项 `.session-item`：标题（双击就地重命名 `.session-rename-input`）+ 时间 + 徽标（`.session-badge`：IF 线标记 / 插图数）+ 悬停删除按钮 `.session-del`（点击弹确认框）。
   - 分组标签 `.session-group-label`（今天/昨天/更早）；搜索态显示命中数。
   - 列表底部渐隐遮罩暗示可滚动。
5. **底部 `.sidebar-foot`**：内核状态芯片（内核 · 已加载/缺失，点击重载）+「设置」按钮（Ctrl+,）。
- 交互：右缘拖拽手柄 `.sidebar-handle` 调宽；拖到最窄或 Ctrl+B 收起；收起后出现 4.6 的进度条；可整体右置（data-sbside=right）。

### 4.3 聊天区 `.chat`（index.html:55-76）
1. **头部 `.chat-header`**：世界线标题（如「序幕 · 帷幕未启」）+ 右侧忙碌状态文字（aria-live）。沉浸布局下隐藏。
2. **搜索栏 `.search-bar`**（Ctrl+F 呼出，styles.css §消息搜索栏）：输入框 + 计数「3/17」+ 上一个/下一个/关闭。命中片段在消息内高亮标记。
3. **消息流 `.messages`**（renderMessages:1214，阅读宽度由 `--read-w` 控制，居中）：
   - **用户消息 `.msg.user`**：角色行「你」+ 时间戳（悬停显示）+ 正文（纯文本）。
   - **世界消息 `.msg.assistant`**：角色行「世界」+ 结构化正文（renderNarrative:1151）：
     - `.scene-line` 场景行：【甲龙历 407.03.01｜清晨｜布耶纳村】——等宽字体、弱色、带边线；
     - `.ask-line` 决定提示：【你需要决定】——accent 强调；
     - `.option-line` 历史选项行（弱化；当前轮已提取为按钮，正文跳过不重复）；
     - `.status-panel` 状态面板：【简要状态】块——等宽字体卡片；
     - 普通段落 `<p>` + 行内 markdown（粗体等）。
   - **消息工具条 `.msg-tools`**（悬停角色行出现）：复制 / 重生成（仅世界）/ 插图·重绘（仅世界且插图就绪）/ **IF 分歧**（仅玩家行动）/ 保存（有插图时）。
   - **插图块**：`.illust`（图片，点击开 Lightbox，Enter/Space 可达）；`.illust-pending`（「正在绘制这一幕的插图」+ 动态省略号）；`.illust-error`（错误 + 「↻ 重试绘制」）。
   - **错误消息**（⚠️ 前缀）：`.msg-body.err` 红调 + 「↻ 重试这一回合」（复用上一条玩家行动重发）。
   - **流式中**：世界消息正文逐字追加 + `▍` 光标；无内容时仅闪烁光标 `.stream-dot`。
4. **空状态 `.empty`**（无消息时，index 中央，app.js:1428）：
   - 菱形 sigil 大图标 + 「六面世界」+「世界已就绪，等待第一个转生者」+ **「开始游戏」主按钮**；
   - 快启出身预设 `.empty-quick`：4 个芯片（平民之子/贵族血脉/…，点击填入输入框可编辑）；
   - 未配置 API 时显示配置提示 `.empty-cfg-tip`（引导去设置/向导）。
5. **回到底部悬浮按钮 `.scroll-bottom`**（上翻时出现于消息流右下）。

### 4.4 选项区 `.choices`（index.html:78-80，app.js:1505-1580，styles.css §选项区）
- **头部行 `.choices-head`**：「这一幕的 3 个选择」+ **多选组合开关 `.multi-toggle`**（开：✓ 多选组合）+ **收起 ▴**。
- **选项按钮 `.choice`**：键位徽章 `.ck`（A/B/C，等宽）+ 选项文本；多选模式下点击变 `.picked`（勾选态），Ctrl+点击随时多选。
- **多选工具条 `.multi-bar`**（有勾选时出现）：「已选 2 项：A + C」+ **组合发送**（主按钮）+ 清空。
- **IF 提示条 `.if-hint`**：首次使用 IF 分歧后出现一次性说明，带「知道了」关闭（localStorage 记忆）。
- **自动收起**：上滑翻历史时整区收起，底部出现「› 展开选项」按钮 `.choices-expand`，回到底部自动展开。

### 4.5 输入区 `.composer`（index.html:82-108，styles.css §输入区，R70 IA 重组）
- **输入框**：2 行 textarea 自增高，placeholder「自由描述你的行动…（Enter 发送 · Shift+Enter 换行）」；空时按 ↑ 召回上一条行动（可连按回溯）。
- **底部两簇**（R70：低频左聚、高频右聚）：
  - **配置簇 `.composer-config`**：文本模型芯片 `.model-chip`（点击开用量面板）+ 插图模型芯片（未启用隐藏）+ 提示文字 `.hint`（如「插图：cogview-4」）+ 模型下拉 `.foot-select`（设置中勾选的清单）+ 思考程度下拉（默认/浅/中/深）。
  - **动作簇 `.composer-actions`**：**灵感**按钮（✦ 图标，从 12 条灵感库随机填入输入框可编辑）+ **发送**主按钮（busy 时变 **STOP** 停止生成）。
- **模型用量面板 `.model-pop`**（renderModelPop:2077，锚定配置簇上方）：分区「文本模型」/「插图模型」，键值行 `.mp-row`：模型、提供商、本线用量（输入/输出/tok）、全部世界线、扣费（无计费时显示「端点未返回计费信息」）；插图区未启用时显示引导。

### 4.6 故事进度条 `.progress-rail`（侧栏完全收起时，styles.css §故事进度条）
- 左缘竖条：节点 `.rail-node`（有插图节点带点）+ 轨道进度填充 `.rail-fill`。
- 悬停节点弹出预览卡 `.rail-pop`：该幕文字摘要（有插图显示图片小窗），点击跳转该幕。

### 4.7 悬浮按钮
- 右下角「?」帮助按钮 `.float-btn.help-btn`（打开指南，Ctrl+/ 直达快捷键页）。

---

## 5. 设置窗口（W2，独立系统窗口，settings.html）

- **自绘标题栏**：标题「设置」+ 重置（恢复默认）/ 最小化 / 最大化 / 关闭；整栏可拖动窗口。
- **Tab 栏 `.modal-tabs`**：文本模型 / 插图模型 / 外观·内核 / 高级。
- **底部**：状态文字 `.set-status`（保存成功/校验错误）+ 取消 / 保存设置。

### Tab 1 文本模型
- 接口：提供商预设下拉（DeepSeek/OpenAI/Kimi/智谱/Qwen/硅基流动/自定义）→ 自动填 baseUrl；API 地址；API 密钥（密码框 + 👁 显隐）；**模型组合框 `.model-combo`**：手填输入框 + 「获取模型」（GET /models）+「测试」+ 下拉面板（筛选框 + 选项列表 + 空态「没有匹配的模型」）+ 测试结果条（成功 ok / 失败 err）。
- 在应用中使用的模型：勾选清单 `.models-pick`（拉取后勾选哪些出现在对话栏下拉）+ 手动添加行（输入 + 添加）。

### Tab 2 插图模型
- 自动插图开关（每段叙事自动生成）；接口（同上结构，预设含「不使用」）；密钥留空复用文本模型。
- 风格与画面：风格预设 7 选（原作轻小说默认/动漫/水彩/油画/水墨/写实/自定义→显示提示词 textarea）；尺寸 7 选（方/16:9 横/3:4/9:16）；清晰度。
- `<details>` 高级生成参数：反向提示词、锁定 seed、生成张数 n。

### Tab 3 外观·内核
- 配色：调色板 7 选 + 明暗 3 选。
- 样式：展示字体 4 选 / 圆角 4 档 / 文字密度 3 档。
- 布局：标准/专注/沉浸；侧栏左/右；置顶开关。
- 阅读：字号 3 档；内容宽度 4 档。
- 世界内核：内核 md 路径 + 「选择…」文件对话框。

### Tab 4 高级
- 上下文条数 / 本地保留条数 / 自动插图最小字数 / 提示词前缀开关+文本。
- 数据管理：清空全部世界线（危险，确认框）/ 导出配置 / 导入配置。

---

## 6. 弹层清单（L1-L11）

| # | 弹层 | 触发 | 结构要点 | 状态/交互 |
|---|---|---|---|---|
| L1 | **画廊 `.gallery`** | 标题栏「画廊」/ Ctrl+G | **右侧抽屉**（保留对话上下文）：头部（标题+计数+关闭）/ 工具栏（世界线切换下拉 + 保存全部插图 + 导出故事存档 HTML）/ 卡片流 `.gallery-card`（图 + meta + 摘要 + 悬停操作：查看/保存/删除） | 空态 `.gallery-empty`；卡片点击开 Lightbox |
| L2 | **大图查看 `.lightbox`** | 点插图/画廊卡 | 全屏黑底 + 图 + 左右导航 + 计数「2/9」+ 保存 + 关闭 | ← → 切换（跨本线全部插图）、Esc 关闭 |
| L3 | **帮助 `.guide`** | ? 按钮 / 首启自动 | 居中模态，双 Tab：怎么玩（4 组 9 步图文）/ 快捷键（分组表 kbd 胶囊） | Esc 关闭 |
| L4 | **主题弹层 `.theme-pop`** | 标题栏「主题」 | 调色板色卡网格 `.swatch`（7 预设，当前勾选）+ 明暗 3 选（跟随系统/深/浅） | 点击即生效（实时预览） |
| L5 | **确认框 `.confirm`** | 删除等危险操作 | 头（标题，danger 前置 ! 圆标）+ 正文 + 底部（取消/确认）；输入型变体 `.confirm-input`（重命名） | Esc=取消；danger 确认钮红底 |
| L6 | **灵动岛通知 `.island`（R76，替代 Toast）** | 全局事件（生成中/完成/失败/保存） | 顶部居中玻璃胶囊：图标圆点 + 文本（+ 副行）+ 操作按钮；插图完成态带缩略图 morph 展开 | busy 呼吸光环+流光线；6s/12s 自动收纳；spring 弹入出。规范见 2.4；参考原型 `scripts-dev/proto/reference-island-cinema.html` |
| L7 | **免责声明** | 首启（向导后） | 520px 模态：5 条条款（滚动区）+ 勾选「我已阅读…」+「同意并继续」（未勾选禁用） | 勾选才可继续；一次性 |
| L8 | **初始化向导 `.confirm.wizard`** | 首启（免责声明前） | 640px×540：欢迎区（渐变「六」logo+欢迎语）→ 圆点步骤条（✓/连接线）→ 三步内容 → 底部（第 X/3 步 + 跳过/上一步 + 下一步→/完成） | ①外观：3 张迷你界面预览卡（CSS 画侧栏+正文小窗），**点击实时切换全界面明暗**，跳过还原；②对话模型：预设卡 4+3（首字头像）+ 地址/密钥/模型（拉取下拉）；③插图：6 预设卡，「暂不启用」显示虚线空状态卡。Enter 前进/Esc 退出；切步方向滑入动画 |
| L9 | **工作区菜单 `.ws-menu`** | 点工作区切换器 | 列表 + meta + 4 操作项（见 4.2） | 点外部关闭 |
| L10 | **消息搜索栏** | Ctrl+F | 见 4.3.2 | Enter/Shift+Enter 导航 |
| L11 | **模型用量面板** | 点模型芯片 | 见 4.5 | 点外部关闭 |

---

## 7. 状态与边界设计盘点（原型需覆盖）

| 类别 | 清单 |
|---|---|
| 加载/忙碌 | 流式光标、STOP 按钮、标题栏忙碌徽标、插图 pending 动画 |
| 错误+恢复 | 消息报错+重试回合、插图失败+重试绘制、端点测试失败、拉取模型失败（向导/设置双处） |
| 空状态 | 消息流空态（开始游戏+出身预设）、画廊空态、模型清单空态、搜索无结果、向导插图空态 |
| 禁用 | 未勾选声明的同意钮、busy 中工具条、默认工作区删除 |
| 自适应 | 窄窗 ≤500px（侧栏自动收起+进度条）；侧栏拖宽范围；内容宽度 4 档 |
| 布局变体 | 专注/沉浸、侧栏右置、字号/密度/圆角/字体 4 轴组合 |
| 主题 | 14 套调色板×明暗（原型至少出 codex 深/浅两套） |

---

## 8. 快捷键全表（帮助面板同步）

Enter 发送 · Shift+Enter 换行 · ↑ 召回上一条 · Ctrl+B 收起侧栏 · Ctrl+N 新世界线 · Ctrl+F 搜索 · Ctrl+, 设置 · Ctrl+G 画廊 · Ctrl+/ 快捷键 · Ctrl+=/-/0 字号 · ←→ Lightbox 切换 · Esc 逐层关闭（Lightbox→搜索→弹层→收起侧栏搜索）

---

## 9. 建议的重设计优先级（给设计 AI 的任务单）

| 优先 | 任务 | 理由 |
|---|---|---|
| P0 | **消息流 + 选项区**（4.3/4.4）：场景行/决定块/状态面板/选项按钮的视觉层级 | 核心循环 90% 时间停留；现有结构化样式偏「代码感」 |
| P0 | **空状态**（4.3.4）：首屏第一印象 | 已有骨架（sigil+CTA+出身芯片），值得品牌化 |
| P0 | **启动入场动画**：Mineradio 式电影开场（暗场光带+字标序列+信号线+粒子尘埃，5.2s 骨架） | 品牌仪式感；配方已从 Mineradio 安装包源码逐帧提取，现成提示词 `design-prompts/p1-splash-entrance.md` |
| P1 | **输入区 composer**（4.5）：配置簇信息密度偏高 | 6 个控件一行，可做分组/折叠 |
| P1 | **初始化向导**（L8）三步 | 刚重做（R75），可做视觉打磨而非重构 |
| P2 | 画廊抽屉（L1）+ Lightbox | 频率中，卡片网格有提升空间 |
| P2 | 设置窗口 4 Tab（W2） | 表单为主，重排分组即可 |
| P3 | 进度条/主题弹层/用量面板 | 小组件微调 |

---

## 10. 设计 AI 投喂提示词模板（复制即用）

> 使用方法：每个界面开一个**新会话**，把下面的模板完整复制，替换 `【】` 占位符后发送。同一界面迭代修改时用 10.3 的反馈提示词，不要重开。

### 10.1 首轮提示词（完整版）

```
# 角色
你是一位资深桌面应用 UI 设计师，擅长极简高信息密度界面（参考 Linear / Codex / Things 的克制风格）。
你正在为一款 Electron 桌面文字 RPG 应用重设计其中一个界面。你只做视觉与排版层设计，不改信息架构。

# 产品与设计语言（必须严格遵守，全程只使用这里的 token）
【粘贴：本文件第 1 章产品概述 + 第 2 章全局设计语言，原样复制】

# 本次任务
重设计界面：【界面名，如「消息流 + 选项区」】

该界面的真实结构清单（元素必须一一保留，一个都不能丢）：
【粘贴：本文件对应章节，如 4.3 + 4.4，原样复制】

该界面必须覆盖的状态：
【粘贴：第 7 章中与该界面相关的行，如 加载/忙碌、错误+恢复、空状态】

该界面的交互与快捷键（保持不变）：
【粘贴：第 8 章，或标注"无"】

# 硬性约束
1. 信息架构不可变：元素不增、不删、不合并、不改文案；层级关系可以重排。
2. 颜色/字体/圆角/投影只允许使用上面 token 清单中的变量；禁止引入新色值。
   若确需新 token（如新的语义色），单独列在"新增 token 提案"中并说明理由，原型中先用近似现有 token。
3. 目标环境：Windows 桌面窗口，标准画布 1120×700，最小可用 500×420（窄窗时侧栏收起）。
4. 深色、浅色两套主题都必须完整呈现，不允许只出深色。
5. 正文阅读区宽度使用 --read-w（720px 档），内容居中。
6. 不引入任何外部字体、图标库、CSS 框架；图标用 16px/1.5px stroke 内联 SVG 或汉字按钮。

# 输出格式（严格按以下三部分输出，顺序固定）
## 第一部分：单文件 HTML 原型
- 输出一个完整可运行的 .html 文件（全部 CSS 内联在 <style>，无外部依赖）。
- 右下角固定一个主题切换按钮，点击切换 html[data-theme="dark"|"light"]，token 变量名与上面清单同名。
- 用真实感的静态演示数据填充（中文叙事文本 2-3 回合、A/B/C 选项、一条插图占位用灰色渐变块表示）。
- 只做这一个界面，不要把其他界面做进来。

## 第二部分：设计说明（≤400 字）
- 改动点列表，每条格式：【改了什么】→【为什么】→【解决清单里的什么问题】
- 使用的 token 清单
- 若有"新增 token 提案"，在此列出

## 第三部分：元素完整性自查表
- 把结构清单中的每个元素逐行列出，标注：保留 / 重排（说明移到了哪里）。
- 这张表用于验收，缺一行视为任务失败。

# 禁止
- 不要输出设计理论、方法论、泛泛的建议。
- 不要改变任何用户可见文案。
- 不要输出多套备选方案，只输出一套你认为最优的。
```

### 10.2 投喂对照表（每次替换哪些占位符）

| 界面任务（第 9 章优先级） | 粘贴章节 | 额外附带 |
|---|---|---|
| **完整体验（推荐首选）** | 无需粘贴（提示词自含全部） | 直接发送 `design-prompts/full-merged.md` 全文：入场动画 + 灵动岛 + 消息流 + 选项区一次出稿 |
| P0 消息流+选项区 | 4.3 + 4.4 | 第 7 章【加载/忙碌、错误+恢复、空状态】行 |
| P0 空状态 | 4.3.4 | 第 7 章【空状态】行 |
| P1 输入区 | 4.5 | 第 7 章【加载/忙碌】行（STOP 态）+ L11 用量面板 |
| P0 启动入场动画 | 无需粘贴章节（提示词已完整自含） | 直接发送 `design-prompts/p1-splash-entrance.md` 全文 |
| P1 初始化向导 | L8 | 第 7 章【禁用】行 |
| P2 画廊+Lightbox | L1 + L2 + 4.7 | 第 7 章【空状态】行 |
| P2 设置窗口 | 第 5 章全文 | — |
| P3 小组件 | 4.6 + L4 + L9 | — |

### 10.3 迭代反馈提示词（同一会话内使用）

```
整体方向保留，只修以下问题（仍然只做视觉层，输出格式同首轮三部分）：
1. 【具体问题，如：场景行与正文的对比太弱，深色下几乎看不见】
2. 【具体问题，如：选项按钮高度建议加大到 ≥36px，键位徽章再突出一点】
3. 【具体问题】
其余部分不要动，直接在上一版 HTML 上修改后完整重新输出。
```

### 10.4 拿到稿之后的验收流程（给我做）

1. 把 AI 输出的 HTML 存到 `scripts-dev/proto/<界面名>.html`，告诉我路径；
2. 我对照本章结构清单逐元素核对（第三部分自查表 + 我方复核），标记：结构缺失 / token 违规 / 状态遗漏；
3. 核对通过后，我把方案翻译成 `styles.css` + `app.js` 的真实改动，并跑全套 e2e 回归（33 套件）确认零破坏。

